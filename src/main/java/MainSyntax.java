public class MainSyntax{
    public static void main(String[] args) {
        System.err.println("public is access modifier!");
        System.err.println("static keyword denotes that method belongs to the Class itself!");
        System.err.println("void is return type which denotes that the method does not return anything!");
        System.err.println("main is a keyword for the method name!");
        System.err.println("String [] args denotes it accepts String arguments!");
    }
}